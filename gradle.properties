# Library version.
# 2.0.0 で JNI ブリッジを捨てて FFM (java.lang.foreign) に移行し、artifactId も
# whisper-jni-custom から whisper-ffm へ変更した。1.x とは座標が違うので共存できる。
version=2.0.0

# whisper.cpp tag the submodule (src/main/native/whisper) is pinned to.
# Informational only - the actual pin lives in git.
whisperCppVersion=1.9.3

# Test dependency versions
junitVersion=5.14.4
